/** @format */

export interface LayoutItems {
  i: string;
  x: number;
  y: number;
  w: number;
  h: number;
  isDraggable: boolean;
  isResizable: boolean;
}

export interface LiveMonitoringProps {
  drawerOpenState: boolean;
}
